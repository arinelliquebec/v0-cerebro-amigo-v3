# URLs dos serviços (dev local — docker usa nomes dos containers)
API_GATEWAY_URL=http://localhost:5050

# Auth BFF
JWT_SECRET=
INTERNAL_API_TOKEN=

# Web Push (público — exposto ao browser)
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
